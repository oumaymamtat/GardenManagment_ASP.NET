@model IEnumerable<Model.Estimate>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutProvider.cshtml";
}

<h2>Estimate</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Item)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Qte)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Total)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Item)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Qte)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Total)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  dateC=item.PkEstimate.DateC , iduser=item.PkEstimate.IdUser, idkinder=item.PkEstimate.IdKinder }) |
          
            @Html.ActionLink("Delete", "Delete", new { dateC = item.PkEstimate.DateC }, new { onclick = "return confirm('Are you sure you want to delete this product?')" })
        </td>
    </tr>
}

</table>
